{
  "name": "raz-portfolio",
  "version": "2.0.0",
  "private": true,
  "description": "Razvan Tirboaca, product designer",
  "scripts": {
    "start": "gulp",
    "build": "gulp build",
    "serve": "gulp build && gulp serve",
    "deploy": "NODE_ENV=production npm run build && git branch --delete --force gh-pages && git checkout --orphan gh-pages && git add -f dist && git commit -m 'build: generates dist' && git filter-branch -f --prune-empty --subdirectory-filter dist && git push -f origin gh-pages && git checkout --force main"
  },
  "author": "Razvan Tirboaca",
  "license": "ISC",
  "browserslist": [
    "> 1%",
    "last 2 versions"
  ],
  "devDependencies": {
    "autoprefixer": "^10.3.1",
    "babel-loader": "^8.2.2",
    "browser-sync": "^2.27.5",
    "del": "^6.0.0",
    "drkmd-js": "^1.0.8",
    "gulp": "^4.0.2",
    "gulp-cache": "^1.1.3",
    "gulp-clean-css": "^4.3.0",
    "gulp-file-include": "^2.3.0",
    "gulp-imagemin": "^7.1.0",
    "gulp-postcss": "^9.0.0",
    "gulp-sass": "^5.0.0",
    "gulp-svg-sprite": "^1.5.0",
    "node-sass-tilde-importer": "^1.0.2",
    "sass": "^1.37.0",
    "stylelint": "^13.13.1",
    "stylelint-config-rational-order": "^0.1.2",
    "stylelint-config-recommended": "^5.0.0",
    "stylelint-config-recommended-scss": "^4.3.0",
    "stylelint-config-standard": "^22.0.0",
    "stylelint-scss": "^3.20.1",
    "webpack-stream": "^6.1.2"
  }
}
