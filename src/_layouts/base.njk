<!DOCTYPE html>
<html {{ site.lang }}} class="h-full bg-primary-strong bg-fixed text-primary-medium font-medium">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} | {{ site.title }}{% else %}{{ site.title }}{% endif %}</title>
    <meta name="title" content="{% if title %}{{ title }} | {{ site.title }}{% else %}{{ site.title }}{% endif %}">
    <meta name="description" content="{% if description %}{{ description }}{% else %}{{ site.description }}{% endif %}">
    <meta name="theme-color" content="#090c07">
    <link rel="stylesheet" href="{{ '/assets/css/main.css' | url }}">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Caveat&family=Figtree:wght@500;700&display=swap" rel="stylesheet">
    {% include 'meta.njk' %}
    {% include 'analytics.njk' %}
</head>
<body class="antialiased h-full flex flex-col">
    <header class="p-8 md:py-12 flex justify-center">
        <!--div class="mx-auto max-w-md md:max-w-lg lg:max-w-6xl pt-8 pb-6 text-center">
            <h1 class="text-xl font-bold">Razvan Tirboaca</h1>
        </div-->
        <figure class="flex items-center gap-4 md:gap-8">
            <img src="assets/images/raz.jpg" alt="Raz Tirboaca" class="w-32 rounded md:rounded-md">
            <figcaption class="flex-1 font-handwriting text-2xl md:text-4xl text-secondary -rotate-2">The thing that matters.. are others<br> Way beyond yourself</figcaption>
        </figure>
    </header>

    {{ content | safe }}

    <footer class="flex justify-center p-6 md:px-8 md:py-16">
        <p class="text-sm text-primary-weak">Based in sunny Barcelona 🇪🇸</p>
    </footer>

</body>
</html>